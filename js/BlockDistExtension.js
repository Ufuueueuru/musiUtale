class BlockDistExtension {
    constructor(dirValue, magnitudeMult, leniency = PI / 2) {
        this.dirValue = dirValue;
        this.magnitudeMult = magnitudeMult;
        this.leniency = leniency;
    }
}