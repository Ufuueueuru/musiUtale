/**
 * This class is used to have separated screens across the game.
 * 
 * There should be a Game screen, and some screens for different menus
 * */
class Screen {
	constructor() {
		this.init();
    }

	draw(g) { }

	run() { }

	init() { }

	loaded() { }
}